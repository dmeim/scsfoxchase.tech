---
import { iconSearch } from '../../scripts/icons';
import './styles.css';

interface Props {
  label: string;
  variant?: "default" | "embedded";
  name?: string;
  value?: string;
  placeholder?: string;
  disabled?: boolean;
  icon?: string;
  inputClass?: string;
  type?: 'search' | 'text';
  class?: string;
  [key: string]: any;
}

const {
  label,
  variant = "default",
  icon = iconSearch,
  inputClass,
  type = 'search',
  class: className,
  ...inputProps
} = Astro.props;
---

<label class:list={['ui-search-field', variant === "embedded" && 'ui-search-field--embedded', className]}>
  {icon && <span set:html={icon}></span>}
  <input class:list={['ui-search-field-input', inputClass]} type={type} aria-label={label} {...inputProps} />
</label>
