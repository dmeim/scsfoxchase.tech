# Pre-Astro static site leftovers
# Moved out of the Astro project root so `astro dev` / deploy serve Astro + public/ only.
# Not copied into dist — safe to keep for reference until Task 9 deletion.

Contents: legacy HTML pages, css/, js/, data/, images/, inventory/, newhome/, oldhome/, old-site/, root sw.js and duplicate PWA/meta files.
